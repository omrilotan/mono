module.exports = function expunge(obj, ...rest) {
	const last = rest[rest.length - 1];
	const options = typeof last === 'object'
		? rest.pop()
		: {}
	;

	switch (typeof by) {
		case 'string':
			break;
		case 'function':
			break;
		default
	}
}
