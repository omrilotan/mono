{
  "name": "fixtures",
  "version": "0.0.0",
  "description": "Dummy fixtures for chunkalyse testing",
  "scripts": {
    "prestart": "rm -rf ./dist && mkdir ./dist",
    "start": "webpack --config webpack.config.js --profile --json > ./dist/stats.json",
    "test": "../bin/chunkalyse.js ./dist/stats.json"
  },
  "dependencies": {
    "@recursive/assign": "^1.2.3",
    "await-reduce": "^1.2.3",
    "paraphrase": "^1.5.0"
  },
  "private": true
}
